declare var globalCanvasRect: DOMRect;
declare var globalCanvasWidth: number;
declare var globalCanvasHeight: number;
declare var selectedTankType: string;

declare var process: {
	env: {
		NODE_ENV: string;
	}
};
